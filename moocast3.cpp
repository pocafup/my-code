#include <iostream>
#include <algorithm>
#include <queue>

using namespace std;
pair<int,int> pos[1001];
int main(){
  int a; cin >> a;
  for (int i=0;i<a;i++) cin >> pos[i].first >> pos[i].second;
  queue<pair<pair<int,int>,int> > q;
  q.push(make_pair(pos[0],0));
  int maxi = 0;
  while(!q.empty()){
    for (int j=0;j<)
  }
}
