#include <iostream>
#include <algorithm>

using namespace std;
#define pp pair<int,int>
#define f first
#define s second
int n;
pp pos[105];
int main(){
  cin >> n;
  for (int i=0;i<n;i++) cin >> pos[i].f >> pos[i].s
}
