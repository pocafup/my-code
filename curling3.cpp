#include <iostream>
#include <algorithm>

using namespace std;
int a;
#define pp pair<int,int>
bool ccw(pp a,pp b){
  return
}
vector<int> convec_hull(pp[] convex){
  sort(convex,convex+a);
  for ()
}
int main(){
  cin >> a;
  for (int i=0;i<a;i++) cin >> cow1[i].first >> cow1[i].second;
  for (int i=0;i<a;i++) cin >> cow2[i].first >> cow2[i].second;

}
