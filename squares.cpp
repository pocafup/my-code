#include <iostream>
#include <algorithm>

using namespace std;

pair<int,int> pos[50001];
int main(){
  int a,b; cin >> a >> b;
  for (int i=0;i<a;i++) cin >> pos[i].first >> pos[i].second;
  sort(pos,pos+a);
  for (int i=0;)
}
