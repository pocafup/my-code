#include <iostream>
#include <algorithm>
#include <vector>

pair<pair<int,int>,pair<int,int> > pos[20001];
int main(){
  int a,b,c; cin >> a >> b >> c;
  vector<pair<pair<int,int>,pair<int,int> > > vec;
  vector<pair<pair<int,int>,pair<int,int> > > vecx;
  for (int i=0;i<c;i++) {
    cin >> pos[i].first.first >>pos[i].first.second >>pos[i].second.first >>pos[i].second.second;
    vecx
  }
}
