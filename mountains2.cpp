#include <iostream>
#include <algorithm>

using namespace std;
int pos[100001];
int height[100001];
void update(int po,bool way, int num){
  int re = way ? 1 : -1;
  if (pos[po]+re>=0 && pos[po]+re<n){
    if (pos[po])
  }
}
int main(){
  int n; cin >> n;
  for (int i=0;i<n;i++) cin >> pos[i].first >> pos[i].second;
  for (int i=0;i<n;i++){

  }
}
